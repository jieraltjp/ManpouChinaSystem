# Prometheus AlertManager 配置
# 指定 AlertManager 实例地址

global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093
