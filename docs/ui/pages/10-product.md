# 页面规格 — 商品目录（步骤X）：商品管理

> **版本**: 1.0.0
> **创建**: 2026-04-23
> **路由**: `/product`
> **组件**: `ProductPage.vue`
> **对应后端**: `Product` 聚合根
> **依赖文档**: `SPEC-B00-全链路总览.md` · `SPEC-B10-商品目录-产品管理.md`

---

## 1. 页面定位

商品主数据管理页面。录入/编辑商品基本信息（货号、品名、规格、HS编码、价格等），并维护商品与工厂的关联关系。商品数据是整个采购链路的起点。

---

## 2. 布局结构

```
┌────────────────────────────────────────────────────────────────────┐
│ 页面标题：商品目录                                  [+ 新规录入]    │
├────────────────────────────────────────────────────────────────────┤
│ 统计卡                                                               │
│ ┌────────────┐                                                    │
│ │ 合计       │                                                    │
│ │ 商品总数   │                                                    │
│ │    N       │                                                    │
│ └────────────┘                                                    │
├────────────────────────────────────────────────────────────────────┤
│ 筛选栏                                                               │
│ 主货号 [________]  品名关键字 [________]  HS编码 [________]        │
│                                              [查询]  [重置]        │
├────────────────────────────────────────────────────────────────────┤
│ 表格                                                               │
│ ┌──────┬────────┬──────────┬──────────┬────────┬──────┬──────┐    │
│ │主货号 │子货号  │品名（中）│品名（英）│HS编码  │单价  │产地  │...│
│ │odn012│        │收纳柜     │Storage  │940320  │¥128  │中国  │    │
│ │       │        │          │Cabinet  │        │      │      │    │
│ └──────┴────────┴──────────┴──────────┴────────┴──────┴──────┘    │
│                                                            [分页]  │
└────────────────────────────────────────────────────────────────────┘
```

---

## 3. 表格列定义

| 列名 | 字段 | 说明 |
|------|------|------|
| 主货号 | `masterCode` | 商品主货号，唯一标识 |
| 子货号 | `subCode` | 颜色/规格变体（可为空） |
| 品名（中） | `nameZh` | 中文品名 |
| 品名（英） | `nameEn` | 英文品名 |
| HS编码 | `hsCode` | 海关商品编码 |
| 单价(RMB) | `unitPriceRmb` | 人民币含税单价，显示 `¥` 前缀 |
| 产地 | `origin` | 产地 |
| 材质 | `material` | 材质 |
| 单位 | `unit` | 计量单位 |
| 操作 | — | 详情 / 编辑 / 删除 |

---

## 4. 新规录入 / 编辑弹窗

### 4.1 触发

- 新规：点击 `[+ 新规录入]` 按钮
- 编辑：表格操作列点击 `[编辑]`

### 4.2 表单字段

| 字段 | 控件 | 必填 | 说明 |
|------|------|------|------|
| 主货号 | `el-input` | ✅ | 最大32字符，唯一键 |
| 子货号 | `el-input` | | 颜色/规格，最大64字符 |
| 品名（中） | `el-input` | ✅ | 最大255字符 |
| 品名（英） | `el-input` | | 最大255字符 |
| 品名（日） | `el-input` | | 最大128字符 |
| HS编码 | `el-input` | | 最大20字符 |
| 单价(RMB) | `el-input-number` | | 精度2位，最小0 |
| 税点 | `el-input-number` | | 默认1.1，精度4位，最小0 |
| 退税率 | `el-input-number` | | 默认0.1，精度4位，最小0最大1 |
| 长(cm) | `el-input-number` | | 精度2位 |
| 宽(cm) | `el-input-number` | | 精度2位 |
| 高(cm) | `el-input-number` | | 精度2位 |
| 净重(kg) | `el-input-number` | | 精度4位 |
| 毛重(kg) | `el-input-number` | | 精度4位 |
| 单位 | `el-input` | | 最大50字符 |
| 产地 | `el-input` | | 最大100字符 |
| 申报要素 | `el-input[type=textarea]` | | 2行 |
| 备注 | `el-input[type=textarea]` | | 2行，最大512字符 |

### 4.3 提交

- 新规：`POST /api/v1/products`
- 编辑：`PATCH /api/v1/products/{id}`

---

## 5. 详情抽屉（右侧滑出，600px）

### 5.1 触发

表格操作列点击 `[详情]`。

### 5.2 内容结构

分三个区：

**基本信息区**
- 主货号 / 子货号 / 品名（中/英/日）/ HS编码

**规格信息区**
- 产地 / 材质 / 单位 / 单价
- 尺寸：长×宽×高 cm（三个值任一有值才显示，否则显示 `—`）
- 重量：净重 / 毛重 kg（两个值任一有值才显示，否则显示 `—`）

**关联工厂区**
- 显示商品关联的工厂列表（来自 `product_factory` 关联表）
- 每个工厂卡片显示：工厂名 / 工厂编码 / 地区 / 联系人 / MOQ / 交期 / 单价
- 首选工厂显示 `el-tag[type=warning]` 标签
- 合作状态显示 `el-tag`
- 无关联工厂时显示"暂无关联工厂"

### 5.3 尾部按钮

- `[关闭]` — 关闭抽屉
- `[编辑]` — 从抽屉进入编辑弹窗

---

## 6. 删除操作

- 操作列 `[删除]` → `ElMessageBox.confirm` 确认框
- 确认后 `DELETE /api/v1/products/{id}`
- 成功提示 + 刷新列表

---

## 7. API 集成

| 操作 | Method | Endpoint |
|------|--------|----------|
| 分页查询 | GET | `/api/v1/products?keyword=&masterCode=&hsCode=&page=&pageSize=` |
| 详情 | GET | `/api/v1/products/{id}` |
| 关联工厂 | GET | `/api/v1/products/{id}/factories` |
| 按货号查询 | GET | `/api/v1/products/code/{masterCode}` |
| 主货号补全 | GET | `/api/v1/products/suggest/master-codes?keyword=` |
| 子货号补全 | GET | `/api/v1/products/suggest/sub-codes?masterCode=` |
| 创建 | POST | `/api/v1/products` |
| 更新 | PATCH | `/api/v1/products/{id}` |
| 删除 | DELETE | `/api/v1/products/{id}` |

---

## 8. 缺口与 TODO

| 项目 | 优先级 | 说明 |
|------|--------|------|
| 商品选择器（补货需求页） | P1 | 已实现 `/suggest/master-codes` 和 `/suggest/sub-codes`，待 DemandPage.vue 集成 |
| 关联工厂增删改 | P1 | 目前只读；待 ProductFactory 增删管理 UI |
| 商品图片上传 | P2 | 目前无图片字段；产品图片 CDN 集成 |
| 批量导入 | P2 | Excel 批量导入商品主数据 |
| 计量单位下拉 | P2 | 目前自由文本；建议改为枚举（个/件/箱/套等） |

---

## 9. 组件拆分建议

| 组件 | 职责 |
|------|------|
| `ProductPage.vue` | 容器：列表 + 筛选 + 统计卡 |
| `ProductFilters.vue` | 筛选栏组件 |
| `ProductTable.vue` | 表格 + 分页 |
| `ProductFormDialog.vue` | 新规/编辑弹窗表单 |
| `ProductDetailDrawer.vue` | 详情抽屉（基本信息 + 规格 + 关联工厂） |
| `ProductFactoryCard.vue` | 关联工厂卡片 |
| `useProduct.ts` | API 调用逻辑（composable） |
