# 测试文档审计报告

> **日期**: 2026-05-01
> **范围**: `docs/test/` + 项目实际测试覆盖
> **严重性**: P0=立即修复 / P1=本周修复 / P2=本月修复 / P3=后续优化

---

## 一、发现的问题

### P0 — 立即修复（Critical）

#### 1. TEST-DATA.md Factory 数据缺少必填字段（P0）

**文件**: `docs/test/TEST-DATA.md` — Section 1

**问题**: 旧文档 Factory 测试数据缺少多个必填字段，无法直接用于插入测试。

| 缺少字段 | 实体要求 | 说明 |
|----------|----------|------|
| `category` | `@NotNull` | FactoryCategory 枚举 |
| `province` | `@NotNull` | 省 |
| `city` | `@NotNull` | 市 |
| `county` | `@NotNull` | 县/区 |
| `paymentTerms` | `@NotNull` | PaymentTerms 枚举 |
| `cooperationStatus` | `@NotNull` | 旧文档用 `status` 字符串，实体用 `cooperationStatus` 枚举 |

**状态**: ✅ 已修复 — 重写 Section 1，新增所有必填字段。

#### 2. TEST-API.md QcRecord POST 缺少 shipmentBatchId（P0）

**文件**: `docs/test/TEST-API.md` — Section 1.2

**问题**: `QcRecordCreateCmd.java` 要求 `@NotNull shipmentBatchId`，但旧文档 POST 请求体中完全没有此字段。开发者按旧文档实现会触发 400 错误。

**状态**: ✅ 已修复 — 新增 `shipmentBatchId: 1` 到请求体，并补充 productCode 必填说明。

#### 3. TEST-API.md QcRecord GET 响应缺少字段（P0）

**文件**: `docs/test/TEST-API.md` — Section 1.1

**问题**: GET 响应缺少 `shipmentBatchId`、`factoryId`、`factoryName`、`qcUserId`，这些在 `QcRecordPageQuery.java` 中存在。

**状态**: ✅ 已修复 — 响应 JSON 中补全所有字段。

---

### P1 — 本周修复（High）

#### 4. TEST-DATA.md Container/ConsolidationPool 数据完全过时（P1）

**文件**: `docs/test/TEST-DATA.md` — Section 5/6

**问题**: 旧文档 Container/ConsolidationPool 测试数据与 v1.5.0 实体完全不匹配：

| 文档字段 | 实际字段 | 问题 |
|----------|----------|------|
| `containerCode` | 不存在 | ❌ |
| `sealNo` | 不存在 | ❌ |
| `planType` | 不存在 | ❌ |
| `estimatedDepartureDate` | 不存在 | ❌ |
| `actualDepartureDate` | `departureDate` | ⚠️ 字段名不同 |
| `destination` | `destinationPort` | ⚠️ 字段名不同 |
| `totalVolume` | `totalCbm` | ⚠️ 字段名不同 |
| `totalWeight` | `totalWeightKg` | ⚠️ 字段名不同 |

**状态**: ✅ 已修复 — Section 5/6 按实际实体完全重写。

#### 5. TEST-API.md LogisticsPlan GET 响应缺少字段（P1）

**文件**: `docs/test/TEST-API.md` — Section 2.1

**问题**: GET 响应缺少 `containerNo`（v1.3.0 新增）、`qcRecordId`、`qcCode`（v1.2.0）、`factoryId`、`factoryName`。

**状态**: ✅ 已修复 — 响应 JSON 中补全所有字段。

#### 6. TEST-API.md LogisticsPlan POST 缺少 factoryId（P1）

**文件**: `docs/test/TEST-API.md` — Section 2.2

**问题**: POST 请求体缺少 `factoryId`，而 `LogisticsPlanCreateCmd` 有此字段。

**状态**: ✅ 已修复 — 请求体中补充 `factoryId`。

---

### P2 — 本月修复（Medium）

#### 7. TEST-STRATEGY.md Testcontainers 描述不精确（P2）

**文件**: `docs/test/TEST-STRATEGY.md` — Section 1.1

**问题**: 旧文档说"Testcontainers 未引入"，实际上父 pom.xml 已有 BOM（但在子模块 manpou-allinone 中未显式引入）。

**状态**: ✅ 已修复 — 补充 Section 1.6 详细说明 Testcontainers 当前状态和引入方法。

#### 8. TEST-API.md 缺少 Container/ConsolidationPool 状态机矩阵（P2）

**文件**: `docs/test/TEST-API.md` — Section 4

**问题**: 旧文档只有 QcRecord 和 LogisticsPlan 状态机，缺少 Container 和 ConsolidationPool 状态机。

**状态**: ✅ 已修复 — Section 4 新增 Container 和 ConsolidationPool 状态机矩阵。

#### 9. TEST-API.md curl 脚本未说明服务端口（P2）

**文件**: `docs/test/TEST-API.md` — Section 3

**问题**: curl 脚本直接写 `localhost:18090`，未说明是 manpou-allinone 端口。

**状态**: ✅ 已修复 — 脚本注释说明是 manpou-allinone 端口。

---

## 二、测试覆盖矩阵

### 后端模块覆盖情况

| Domain | UseCase | 测试文件 | 覆盖 | FSM | 业务计算 |
|--------|---------|---------|------|-----|---------|
| procurement | ProcurementUseCase | `ProcurementUseCaseTest.java` | ✅ | ✅ | ✅ |
| factory | FactoryUseCase | `FactoryUseCaseTest.java` | ✅ | ✅ | - |
| qc | QcRecordUseCase | `QcRecordUseCaseTest.java` | ✅ | ✅ | - |
| logistics | LogisticsPlanUseCase | `LogisticsPlanUseCaseTest.java` | ✅ | ✅ | - |
| replenishment | ReplenishmentDemandUseCase | `ReplenishmentDemandUseCaseTest.java` | ✅ | - | - |
| logistics | ContainerUseCase | ❌ 无测试 | ❌ | ❌ | ❌ |
| logistics | ConsolidationPoolUseCase | ❌ 无测试 | ❌ | ❌ | ❌ |
| customs | CustomsDeclarationUseCase | ❌ 无测试 | ❌ | ❌ | ❌ |
| customs | JapanCustomsUseCase | ❌ 无测试 | ❌ | ❌ | ❌ |
| finance | FinanceSettlementUseCase | ❌ 无测试 | ❌ | ❌ | ❌ |
| finance | TaxRefundUseCase | ❌ 无测试 | ❌ | ❌ | ❌ |
| product | ProductUseCase | ❌ 无测试 | ❌ | ❌ | ❌ |
| sales | SalesRecordUseCase | ❌ 无测试 | ❌ | ❌ | ❌ |
| warehouse | WarehouseUseCase | ❌ 无测试 | ❌ | ❌ | ❌ |
| notification | NotificationUseCase | ❌ 无测试 | ❌ | ❌ | ❌ |
| api-gateway | JwtValidator | `JwtValidatorTest.java` | ✅ | - | - |

**覆盖率**: 5/16 模块 (31%)，9 个模块零覆盖。

### 前端覆盖情况

| 类型 | 数量 | 测试状态 |
|------|------|---------|
| Composables | 3+ | ❌ 0 tests |
| Pinia Stores | 1 (auth) | ❌ 0 tests |
| Vue Components | 50+ | ❌ 0 tests |
| API Adapters | 10+ | ❌ 0 tests |
| 工具函数 | ? | ❌ 0 tests |

**覆盖率**: 0%。

---

## 三、TEST-DATA.md vs 实体字段对照

| 实体 | 文档状态 | 主要问题 |
|------|----------|----------|
| Factory | ✅ 已修正 | category/province/city/county/paymentTerms/cooperationStatus 字段已补全 |
| Procurement | ✅ 正确 | 无问题 |
| QcRecord | ✅ 已修正 | shipmentBatchId 必填字段已补全 |
| LogisticsPlan | ✅ 已修正 | qcRecordId 字段已补全 |
| Container | ✅ 已修正 | 按 v1.5.0 实体完全重写 |
| ConsolidationPool | ✅ 已修正 | destinationPort/totalCbm/totalWeightKg 字段已修正 |

---

## 四、Maven Surefire 配置验证

**包含模式**（`apps/java-service/pom.xml` Line 447-449）:
```xml
<include>**/*Test.java</include>
<include>**/*Tests.java</include>
```

✅ `*UseCaseTest.java` 文件会被 `**/*Test.java` 匹配（因为以 `Test.java` 结尾），不会被跳过。

**CI Profile ArchUnit 排除**（`apps/java-service/pom.xml` Line 601-603）:
```xml
<exclude>**/arch/*Test.java</exclude>
```

⚠️ CI 环境跳过 ArchUnit 架构测试。本地 `mvn test` 执行完整测试。

---

## 五、立即行动清单

### 已完成（本次审计修复）

- [x] TEST-DATA.md Factory 必填字段补全
- [x] TEST-API.md QcRecord POST shipmentBatchId 补全
- [x] TEST-API.md QcRecord GET 响应字段补全
- [x] TEST-DATA.md Container/ConsolidationPool 数据重写
- [x] TEST-API.md LogisticsPlan GET 响应字段补全
- [x] TEST-API.md LogisticsPlan POST factoryId 补全
- [x] TEST-STRATEGY.md Testcontainers 状态说明
- [x] TEST-API.md Container/ConsolidationPool 状态机矩阵

### 待完成

| 优先级 | 任务 | 影响 |
|--------|------|------|
| P1 | 编写 `ContainerUseCaseTest` | 覆盖 logistics Container FSM + addPlan 逻辑 |
| P1 | 编写 `ConsolidationPoolUseCaseTest` | 覆盖 ConsolidationPool isReadyToLoad / addPlan / 状态机 |
| P1 | 在 manpou-allinone/pom.xml 引入 Testcontainers 依赖 | 为 CI 集成测试做准备 |
| P2 | 前端安装 Vitest + Playwright | 建立测试基础设施 |
| P2 | 编写 `useAuth` composable 测试 | 前端首个单元测试 |
| P2 | 编写 CustomsDeclarationUseCaseTest | 报关模块覆盖 |
| P2 | 编写 FinanceSettlementUseCaseTest | 财务模块覆盖 |
| P3 | 编写 ProductUseCaseTest | 商品模块覆盖 |
| P3 | 编写 WarehouseUseCaseTest | 仓储模块覆盖 |
| P3 | 编写 SalesRecordUseCaseTest | 销售模块覆盖 |
| P3 | 前端组件测试 | 关键 UI 组件 |
