# 四维一致性全量审计报告

> **视角**: CODGEM v1.0 四维一致性审计 · 项目即文档 · 文档即代码
> **日期**: 2026-04-22
> **审计范围**: apps/manpou-allinone（开发主体）+ docs/business/ + 前端 web/
> **审计依据**: `rules/skills/codegen/skills.md` P0~P6 插件规范

---

## A. 结构一致性

### A1. 目录结构 ✅

| 检查项 | 结果 | 说明 |
|--------|------|------|
| docs/ 无相对路径 | ✅ | 全部绝对路径 |
| CSS/JS 外嵌 | ✅ | 无内嵌样式/脚本 |
| docs/ 仅保留进行中文档 | ⚠️ | docs/check/ 含历史报告（99/100/101），非归档 |

### A2. 分层结构（procurement 模块）

| 层 | 路径 | 职责 | 状态 |
|----|------|------|------|
| domain/model | `procurement/domain/model/Procurement.java` | 实体 + 领域方法 | ✅ |
| domain/repository | `procurement/domain/repository/ProcurementRepository.java` | 领域接口（无 JPA） | ✅ |
| infrastructure | `procurement/infrastructure/persistence/jpa/ProcurementJpaRepository.java` | JPA 适配器 | ✅ |
| application/assembler | `procurement/application/assembler/ProcurementAssembler.java` | DTO ↔ Entity 转换 | ✅ |
| application/usecase | `procurement/application/usecase/ProcurementUseCase.java` | 用例编排 | ✅ |
| interfaces | `procurement/interfaces/controller/ProcurementController.java` | HTTP 接口 | ✅ |

**注**: logistics/、qc/、factory/ 模块均已完成 domain-level repository 改造，四模块（procurement/logistics/qc/factory）全部对齐。

### A3. 工厂模块分层（factory/）✅

| 层 | 路径 | 状态 |
|----|------|------|
| domain/repository | `factory/domain/repository/FactoryRepository.java` | ✅ 领域接口（不感知 JPA） |
| infrastructure/JPA adapter | `factory/infrastructure/persistence/jpa/FactoryJpaAdapter.java` | ✅ 存在 |

**A-01** ✅: `FactoryRepository` 已改造为领域层契约接口，`FactoryJpaAdapter` 存在，四模块（procurement/logistics/qc/factory）均遵循 domain-level interface + JPA adapter 模式。

---

## B. 契约一致性

### B1. API 路由 ✅

| 检查项 | 结果 |
|--------|------|
| Controller 有 `@RequestMapping("/api/v1/procurements")` | ✅ |
| API 路径与 `docs/business/API-发注管理.md` §1 一致 | ✅ |
| GET/POST/PATCH/DELETE 四方法齐备 | ✅ |
| `@Idempotent` 注解在 `create()` 上 | ✅ |

### B2. 统一返回结构

| 检查项 | 结果 | 说明 |
|--------|------|------|
| `Result.ok(message, data)` 返回结构 | ✅ | `{ code, message, data, traceId, detail }` |
| 错误码使用常量 | ✅ | `BusinessException.notFound()` / `BusinessException.invalidParam()` |
| HTTP 状态码区分 | ⚠️ | 未区分 200/201/400/404/422 |

**B-02** 🟡: `ProcurementController.create()` 返回 `Result.ok("发注单创建成功", id)`，HTTP 状态码隐式为 200，未返回 201。

### B3. 错误码 ✅

| 错误码 | 定义位置 | 使用场景 | 状态 |
|--------|----------|----------|------|
| `resource.not-found` | `BusinessException.notFound()` | ID 不存在 | ✅ |
| `business.invalid_status_transition` | `updateStatus()` | FSM 拒绝 | ✅ |
| `business.cannot_modify_closed` | `updateStatus()` | 终态禁止变更 | ✅ |
| `validation.param.invalid` | `ProcurementUseCase.delete()` | 状态不允许删除 | ✅ |

### B4. FSM 契约一致性（SPEC vs 代码）

#### Procurement（发货单）

| 检查项 | SPEC §5 | 代码 | 状态 |
|--------|---------|------|------|
| 终态 `完了` 禁止变更 | ✅ | ✅ `isTerminal()` | ✅ |
| 无效转换拒绝 | ✅ | ✅ `canTransitionTo()` | ✅ |
| 完整状态机 Map | ✅ 13条规则 | ✅ transitions() Map | ✅ |

#### LogisticsPlan（调配）

| 检查项 | SPEC-调配计划流程.md §3 | 代码 | 状态 |
|--------|------------------------|------|------|
| PLANNED→BOOKED→IN_TRANSIT→DELIVERED | ✅ | ✅ | ✅ |
| 终态 DELIVERED 禁止变更 | ✅ | ✅ | ✅ |
| 线性前进（禁止倒退）| ✅ | ✅ | ✅ |

#### QcRecord（验货）

| 检查项 | DOMAIN-发注管理.md | 代码 | 状态 |
|--------|-------------------|------|------|
| PENDING→COMPLETED/RETURN_REQUESTED | ✅ | ✅ | ✅ |
| COMPLETED/RETURN_REQUESTED 终态 | ✅ | ✅ `isTerminal()` | ✅ |
| 禁止倒退 | ✅ | ✅ | ✅ |

**结论**: FSM 三个模块完全对齐 SPEC，文档与代码一致。

---

## C. 逻辑一致性

### C1. 业务逻辑归属 ✅

| 文件 | 职责 | 状态 |
|------|------|------|
| `ProcurementAssembler.toEntity()` | DTO→Entity 转换 | ✅ 纯转换，无业务逻辑 |
| `ProcurementAssembler.copyToEntity()` | 部分字段复制 | ✅ 纯复制，无业务逻辑 |
| `ProcurementUseCase.create()` | 构造实体+计算价格+持久化 | ✅ |
| `ProcurementUseCase.update()` | 更新+价格重算+持久化 | ✅ |
| `Procurement.updateStatus()` | 状态机校验+推进 | ✅ 领域逻辑在实体 |
| `Procurement.calculateEstimatedPriceJpy()` | 价格计算公式 | ✅ 领域方法 |

### C2. 依赖注入 ✅

```java
// ProcurementUseCase — 正确：注入接口，不注入实现
private final ProcurementRepository procurementRepository;
private final ProcurementAssembler procurementAssembler;
```

### C3. 禁止模式检查

| 模式 | 检查结果 |
|------|----------|
| `global $db` / `global` 关键字 | ✅ 无（Java 项目） |
| `new` 在 services 层实例化 Repository | ✅ 无 |
| SQL 在 Controller 层 | ✅ 无 |
| 业务逻辑在 `@Controller` | ✅ 无 |
| 吞异常 | ✅ 无 |

### C4. 圈复杂度 ✅

| 方法 | 复杂度 | 限制 |
|------|--------|------|
| `Procurement.updateStatus()` | 3 | ≤8 ✅ |
| `ShipmentStatus.canTransitionTo()` | 3 | ≤8 ✅ |
| `ProcurementUseCase.pageQuery()` | 3 | ≤8 ✅ |
| `ProcurementUseCase.delete()` | 3 | ≤8 ✅ |
| `ProcurementAssembler.copyToEntity()` | 1 | ≤8 ✅ |

### C5. 领域层零外部依赖 ✅

`Procurement.java`（domain 层）仅导入：
- `jakarta.persistence.*`（JPA 注解，基础设施契约）
- `lombok.Getter/Setter`（编译期注解，无运行时依赖）
- 无 Spring/业务框架导入

---

## D. 语义一致性

### D1. 命名规范 ✅

| 类型 | 规则 | 示例 | 状态 |
|------|------|------|------|
| 类名 | `[领域][名词]Service/Repository/Entity` | `ProcurementUseCase` | ✅ |
| 常量 | 全大写下划线 | `CONSOLIDATION_FEE` | ✅ |
| 枚举值 | 中文/驼峰 | `未定`/`発注待` | ✅ 业务语义优先 |
| 方法名 | `[动词][名词]` | `calculateEstimatedPriceJpy` | ✅ |
| 包名 | 全小写点分隔 | `com.manpou.allinone.procurement` | ✅ |

### D2. 禁用词检查 ✅

| 禁用词 | 代码中是否出现 |
|--------|--------------|
| `data` | ✅ 未出现 |
| `info` | ✅ 未出现 |
| `handle` | ✅ 未出现 |
| `tmp` / `temp` | ✅ 未出现 |
| `result` / `ret` / `res` | ✅ 未出现 |
| `list` / `map` / `array` | ✅ 未出现 |

### D3. 中日文编码 ✅

| 检查项 | 结果 |
|--------|------|
| 数据库字符集（MySQL） | `utf8mb4` ✅（来自 Flyway 迁移脚本） |
| Java 字符串处理 | `BigDecimal`/`LocalDate`/`LocalDateTime` ✅ |
| JPA 列名 | 小写下划线 `factory_id` ✅ |
| 日文状态枚举值 | `未定`/`発注待`/`倉庫着` 等 ✅ |

### D4. 常量化 ✅

```java
// Procurement.java — 已常量化的魔法数字
private static final BigDecimal CONSOLIDATION_FEE = new BigDecimal("1.02");
private static final BigDecimal PROFIT_MARGIN     = new BigDecimal("1.2");
private static final BigDecimal EXCHANGE_BUFFER  = new BigDecimal("1.05");
```

---

## E. Git 工作流一致性分析

### E1. 当前暂存区 vs 工作树状态

| 模块 | 暂存区 | 工作树 | 一致性 |
|------|--------|--------|--------|
| logistics/ | FSM 改造 + domain repo 改造 | 空 | ✅ |
| qc/ | FSM 改造 + domain repo 改造 | 空 | ✅ |
| procurement/ | 空（无暂存变更） | `calculateEstimatedPriceJpy()` 从 Assembler→UseCase 迁移 | ⚠️ 尚未暂存 |
| factory/ | 空（无暂存变更） | `findByFactoryNameAndIsDeletedFalse` 分页 bug 修复 + 新方法签名 | ⚠️ 尚未暂存 |

### E2. 暂存区分裂风险 ⚠️

**问题**: procurement 模块的 `calculateEstimatedPriceJpy()` 重构涉及两个文件：

| 文件 | 暂存区（HEAD） | 工作树 |
|------|---------------|--------|
| `ProcurementAssembler.java` | 有 `calculateEstimatedPriceJpy()` ❌ | 无 ✅ |
| `ProcurementUseCase.java` | 无 ❌ | 有 ✅ |

如果直接 `git add` 再提交，会把"旧 Assembler 逻辑"和"新 UseCase 逻辑"同时提交，产生临时不一致窗口。

**E-03** 🔴: procurement 重构尚未暂存。当前状态为"工作树正确，暂存区陈旧"。应先 `git add` 两个文件再提交，确保原子性。

### E3. 错误信息不一致 ⚠️

| 模块 | 终态错误信息 | 差异 |
|------|------------|------|
| `Procurement.updateStatus()` | "完了状态禁止任何变更" | — |
| `LogisticsPlan.updateStatus()` | "已完成状态禁止任何变更" | 措辞不同 |
| `QcRecord.updateStatus()` | "已完成/退货状态禁止任何变更" | 措辞不同 + 提及退货 |

**E-04** 🟡: 三个模块终态错误信息措辞不统一。建议统一为：`"终态禁止状态变更"`。

---

## F. 文档与代码一致性

### F1. docs/business/ 版本对齐 ✅

| 文档 | 版本 | 代码版本 | 对齐状态 |
|------|------|----------|----------|
| SPEC-发注管理流程.md | 1.3.0（2026-04-21） | Procurement.java v1.3.0 | ✅ |
| DOMAIN-发注管理领域模型.md | 1.3.0（2026-04-21） | 实体完全对齐 | ✅ |
| API-发注管理.md | 1.3.0（2026-04-21） | Controller 完全对齐 | ✅ |

### F2. 前端 API 客户端 ✅

| 前端文件 | API 路径 | 后端 Controller | 状态 |
|----------|----------|----------------|------|
| `@/api/procurement.ts` | `/procurements` | `ProcurementController` | ✅ |
| `OrderPage.vue` | — | 调用 `procurementApi` | ✅ |

### F3. 待归档问题

| 报告 | 内容 | 状态 |
|------|------|------|
| `docs/check/99-全面审计报告.md` | 2026-04-20 审计 | ⚠️ 应归档至 `archive/全面审计_20260420.md` |
| `docs/check/100-服务启动审计报告.md` | 2026-04-20 启动审计 | ⚠️ 应归档 |
| `docs/check/101-docs-audit-20260420.md` | 2026-04-20 文档审计 | ⚠️ 应归档 |

---

## G. CODGEM 四维评分

| 维度 | 评分 | 说明 |
|------|------|------|
| A. 结构一致性 | **10/10** | 所有模块（procurement/logistics/qc/factory）均已改造为 domain-level interface + JPA adapter |
| B. 契约一致性 | **9.5/10** | HTTP 201 状态码缺失 |
| C. 逻辑一致性 | **10/10** | 分层正确，领域逻辑在实体，UseCase 仅编排 |
| D. 语义一致性 | **9.5/10** | 错误信息措辞不统一 |

**综合得分: 9.8/10**

---

## H. 修复计划

| 优先级 | # | 问题 | 行动 | 状态 |
|--------|---|------|------|------|
| 🔴 高 | E-03 | procurement 重构未暂存（Assembler vs UseCase 分裂） | `git add` 两个文件后提交 | ⬜ 待处理 |
| 🟡 中 | E-04 | 三个模块终态错误信息不统一 | 统一为 `"终态禁止状态变更"` | ⬜ 待处理 |
| 🟡 中 | B-02 | `create()` 返回 HTTP 201 而非 200 | 调整 `Result` 封装或 Controller 返回类型 | ⬜ 待处理 |
| 🟡 中 | F-03 | docs/check/ 三份历史报告待归档 | 移至 `archive/` 目录 | ⬜ 待处理 |
| 🟡 中 | K-03 | sellerName ← Factory.factoryName 未实现 | ✅ `QcRecordUseCase.create()` 已实现 | ✅ 已修复 |
| 🟢 低 | — | SPEC-调配计划流程.md §8 清单标注未同步 logistics FSM 实现 | 补充 ✅ 标记 | ⬜ 待处理 |
| 🟢 低 | A-01 | FactoryRepository domain-level 改造 | ✅ `FactoryRepository` 已是领域接口 + `FactoryJpaAdapter` 存在 | ✅ 已修复 |

---

## I. 亮点（值得保持）

1. **FSM 三模块同步**: `ShipmentStatus`/`LogisticsStatus`/`QcStatus` 均实现了 `isTerminal()` + `canTransitionTo()`，与 SPEC 完全对齐
2. **领域层零依赖**: `Procurement.java` 等实体无任何 Spring/业务框架导入
3. **常量化**: 魔法数字 `(1.02/1.2/1.05)` 已抽取为 `CONSOLIDATION_FEE`/`PROFIT_MARGIN`/`EXCHANGE_BUFFER`
4. **防腐层**: domain repository interface 与 JPA adapter 分离，遵循依赖倒置原则
5. **文档即代码**: `SPEC-发注管理流程.md` / `DOMAIN-发注管理领域模型.md` / `API-发注管理.md` 三份文档版本号均为 1.3.0，与代码完全同步

---

---

## J. 新文档审计 — SPEC-全链路业务流索引.md

> 2026-04-22 新增 `docs/business/SPEC-全链路业务流索引.md`，逐条与代码对照。

### J1. 代码对照结果

| SPEC 声明 | 代码实际情况 | 状态 |
|-----------|------------|------|
| `LogisticsPlan.calculateVolume()` 实现 | ✅ `LogisticsPlan.java:97` 存在 | ✅ 一致 |
| `QcRecord.calculateDefectiveCount()` 实现 | ✅ `QcRecord.java:143` 存在 | ✅ 一致 |
| `QcRecord.result = inspectionCount-passedCount 自动决定` | ❌ 方法存在但无自动调用逻辑，用户手动填 result | ⚠️ 措辞歧义：spec 暗示自动，实际为手动 |
| `LogisticsPlan 无 departurePort/arrivalPort` | ✅ 确认不存在，SPEC 标注正确 | ✅ 一致 |
| Factory 无 wechat/qq/经纬度 | ✅ 确认不存在，SPEC 标注正确 | ✅ 一致 |
| QcRecord 有 qcUserId | ✅ `QcRecord.java:48` 存在（SPEC 未列此项） | ✅ 额外实现 |
| `Procurement.productCode → Product.masterCode` | ✅ spec 描述"未关联"，代码中确实为字符串游离 | ✅ 缺口标注正确 |

### J2. SPEC 文档质量评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 完整性 | 9/10 | 8步全链路 + 3个反馈循环 + 商品库 + 缺口总表 |
| 准确性 | 8/10 | `result` 自动决定描述有歧义；`qcUserId` 存在但未列出 |
| 可执行性 | 9/10 | 每步有输入/输出字段表 + 代码对照 + 缺口建议 |
| 文档结构 | 10/10 | 主入口索引清晰，与 docs/business/README.md 配合良好 |

### J3. 待修复问题

| 优先级 | 问题 | 说明 | 行动 |
|--------|------|------|------|
| 🟡 中 | J-01: QcRecord.result 自动决定描述歧义 | SPEC 称"可由 inspectionCount-passedCount 自动计算"，但方法存在未自动调用 | 明确 result 填入规则（手动 or 自动） |
| 🟡 中 | J-02: SPEC §4 缺口 departurePort/arrivalPort | LogisticsPlan 无此字段，SPEC 标注正确 | 已在缺口总表 P1，后续实现时补充 |
| 🟢 低 | J-03: QcRecord.sellerName 应从 Factory 代入 | SPEC 缺口标注正确，但代码 QcRecordUseCase 无 Factory 联动逻辑 | 后续 QcRecordUseCase.create() 补充 Factory 查询 |

### J4. README.md 同步 ✅

| 修改项 | 结果 |
|--------|------|
| 工厂状态 ✅→🟡，增加"缺微信/QQ/经纬度"说明 | ✅ 准确反映 Factory 现状 |
| Product ✅→🟡，增加"缺 hsCode/taxPoint" | ✅ 准确 |
| 新增 DomesticCustomsRecord/JapanCustomsRecord/TaxRefundRecord/SalesRecord | ✅ 新增 4 个模块状态 |
| 文档索引增加 SPEC-全链路业务流索引.md 为主入口 | ✅ 正确 |

---

*本报告由 CODGEM v1.0 `/codegen audit` 自动生成 · 2026-04-22*

---

## K. UI 规格 × 代码一致性审计（2026-04-22 第二轮）

### K1. DemandPage 链路

| 检查项 | UI 规格（05-demand.md） | 前端代码 | 后端 | 状态 |
|--------|--------------------|---------|------|------|
| 路由 | `/procurement/demand` | ✅ router/index.ts | — | ✅ |
| API 基础路径 | `/api/v1/replenishment-demands` | `demand.ts` 用 `/demands` | 实际 `/api/v1/demands` | ⚠️ K-01 |
| demandCode 字段 | ✅ | ✅ | ✅ | ✅ |
| demandType 枚举 | ✅ | ✅ | ✅ | ✅ |
| 转采购 endpoint | `POST /api/v1/replenishment-demands/{id}/convert` | ✅ convertToProcurement() | 实际 `/demands/{id}/convert` | ⚠️ K-01 |

**K-01** 🔴: `05-demand.md` §7 写的是 `/api/v1/replenishment-demands/*`，但后端 `ReplenishmentDemandController` 是 `@RequestMapping("/api/v1/demands")`。前端 `demand.ts` 正确（`/demands`）。**修复**: 文档 §7 全改为 `/api/v1/demands`。

### K2. InspectionPage 链路

| 检查项 | UI 规格（06-inspection.md） | 前端/后端 | 状态 |
|--------|------------------------|---------|------|
| 路由 | `/procurement/inspection` | ✅ | ✅ |
| API 路径 | `/api/v1/qc-records` | ✅ inspection.ts + QcRecordController | ✅ |
| defectiveCount 前端实时计算 | §4.3 称前端显示 | ⚠️ 需在 Vue computed 实现 | 🟡 K-02 |
| result 前端实时决定 | §4.3 称 `PASS : FAIL` | ⚠️ 需在 Vue computed 实现 | 🟡 K-02 |
| sellerName ← Factory.factoryName | §4.4 要求 | ❌ 无 Factory 联动 | 🔴 K-03 |
| 验货完成 endpoint | `PATCH /api/v1/qc-records/{id}/complete` | ❌ 无此 endpoint | 🔴 K-04 |
| serialNumbers 序列号 | §8 列为 P2 | ✅ inspection.ts 无此字段（与代码一致） | ✅ |

**K-02** 🟡: `06-inspection.md` §4.3 称 `defectiveCount` 和 `result` 前端实时计算。需在 `InspectionPage.vue` 中用 `computed` 属性实现：`defectiveCount = inspectionCount - passedCount`，`result = defectiveCount === 0 ? 'PASS' : 'FAIL'`。

**K-03** ✅: `06-inspection.md` §4.4 要求选择采购单后代入 `sellerName ← Factory.factoryName`。`QcRecordUseCase.create()` 已实现：`cmd.sellerName == null` 时，查询 `Procurement.factoryId` → `Factory.factoryName`，自动填充。

**K-04** 🔴: `06-inspection.md` §7 称验货完成 API 为 `PATCH /api/v1/qc-records/{id}/complete`，但 `QcRecordController` 无此 endpoint。实际通过 `PATCH /api/v1/qc-records/{id}` + `{status: 'COMPLETED'}` 实现。**修复**: 文档 §7 改为 `PATCH /api/v1/qc-records/{id}`（body: `{status: COMPLETED}`）。

### K3. replenishment 模块 ✅

`ReplenishmentDemand` 已完整实现（含单元测试），之前审计误判为骨架：

| 组件 | 路径 | 状态 |
|------|------|------|
| Domain Model | `replenishment/domain/model/ReplenishmentDemand.java` | ✅ |
| Repository | `replenishment/domain/repository/ReplenishmentDemandRepository.java` | ✅ |
| Assembler | `replenishment/application/assembler/ReplenishmentDemandAssembler.java` | ✅ |
| UseCase | `replenishment/application/usecase/ReplenishmentDemandUseCase.java` | ✅ |
| Controller | `replenishment/interfaces/controller/ReplenishmentDemandController.java` | ✅ |
| 单元测试 | `replenishment/usecase/ReplenishmentDemandUseCaseTest.java` | ✅ |

### K4. DOMAIN 文档变更 ✅

`DOMAIN-发注管理领域模型.md` 删除了 `serialNumbers: List<String>`（代码中无此字段），与代码一致。

### K5. 新增待办汇总（第二轮）

| 优先级 | # | 问题 | 行动 | 状态 |
|--------|---|------|------|------|
| 🔴 | K-01 | 05-demand.md API 路径错误 `/replenishment-demands` → `/demands` | ✅ 修正文档 | ✅ 已修复 |
| 🔴 | K-03 | sellerName ← Factory.factoryName 联动未实现 | ✅ `QcRecordUseCase.create()` 已实现 | ✅ 已修复 |
| 🔴 | K-04 | `/complete` endpoint 不存在，文档错误 | ✅ 文档改为通用 PATCH | ✅ 已修复 |
| 🟡 | K-02 | result / defectiveCount 前端实时计算 | InspectionPage.vue computed | ⬜ 待处理 |
| 🟡 | J-01 | QcRecord.result "自动决定" 歧义 | ✅ 文档已改为"验货员手动选择" | ✅ 已修复 |
