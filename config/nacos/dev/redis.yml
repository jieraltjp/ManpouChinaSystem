# ============================================================
# Redis 配置（dev 环境）
# ============================================================
spring:
  data:
    redis:
      host: redis
      port: 6379
      password: redis123
      database: 0
      timeout: 5000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 2
          max-wait: -1ms
        # 集群模式（生产环境使用）
        # cluster:
        #   nodes: ${REDIS_CLUSTER_NODES}
