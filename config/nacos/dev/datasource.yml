# ============================================================
# 数据源配置（dev 环境）
# ============================================================
spring:
  datasource:
    url: jdbc:mysql://192.168.13.202:23306/${DB_NAME:app}?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: manpou23306
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 20000
      idle-timeout: 300000
      max-lifetime: 1200000
      pool-name: HikariPool-Dev

  # H2 本地开发（替代 TiDB，不需要 Docker）
  # 启用方式：取消下面的注释，并注释掉上面的 MySQL 配置
  # datasource:
  #   url: jdbc:h2:mem:${DB_NAME:app};MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
  #   username: sa
  #   password:
  #   driver-class-name: org.h2.Driver
  # jpa:
  #   hibernate:
  #     ddl-auto: update
  #   properties:
  #     hibernate:
  #       dialect: org.hibernate.dialect.H2Dialect

  # Flyway 迁移（dev 环境可开启自动 clean）
  flyway:
    enabled: true
    baseline-on-migrate: true
    clean-disabled: false
